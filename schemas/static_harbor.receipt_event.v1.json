{
  "schema": "static_harbor.receipt_event.v1",
  "type": "object",
  "required": [
    "schema",
    "ts_utc",
    "event_type",
    "event_sha256",
    "prev_receipt_sha256",
    "receipt_sha256"
  ],
  "properties": {
    "schema": { "type": "string" },
    "ts_utc": { "type": "string" },
    "event_type": { "type": "string" },
    "event_sha256": { "type": "string" },
    "prev_receipt_sha256": { "type": ["string","null"] },
    "receipt_sha256": { "type": "string" }
  }
}
